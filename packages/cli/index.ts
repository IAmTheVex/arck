import "@arck/core";

import * as Interface from "@oclif/command";

import * as Wrapper from "./wrapper";

const Cli = {
    Interface,
    Wrapper
};

export {
    Interface,
    Wrapper,

    Cli
};

export default Cli;